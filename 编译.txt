$env:CGO_ENABLED="1"
$env:CC="D:\MSYS2\ucrt64\bin\gcc.exe"

# 指向 GPU 版 onnxruntime.dll（以及确保 CUDA 相关依赖在同目录）
$env:ONNXRUNTIME_SHARED_LIBRARY_PATH="D:\go\ddddocr_go\gpu\onnxruntime.dll"
go env -w GOCACHE="C:\GoBuild"
$env:TMP  = "C:\GoTemp"
go build -tags onnx -ldflags "-s -w" -gcflags "all=-trimpath=${PWD}" -asmflags "all=-trimpath=${PWD}" -o hexxagon.exe .\cmd\hexxagon\main.go